en:
  card_empty_data_component:
    title: No data found

  export_dropdown_component:
    export:
      current_page: "Export the current page"
      all_pages: "Export everything"

  changelog_entries:
    object_list_component:
      loading: Loading ...

  filter_component:
    submit: Filter
    reset: Reset%{filters_count}
    total_with_filters: "Total: %{results_count} on %{total_count} results"
    total: "Total: %{count}"

  form_errors_component:
    title:
      one: "1 error occured while saving:"
      other: "%{count} errors occured while saving:"

  has_many_turbo_frame_component:
    loading: Loading ...
    see_more_filters: See more filters

  list:
    data_table_component:
      bulk:
        selected_elements: selected item(s)

  overview:
    create_delete_button_component:
      delete_button:
        title: Delete empty bay
        confirm: Are you sure you want to delete this bay?
      create_button:
        title: Add a frame

  pagination_component:
    items_per_page: Items per page

  columns_preferences_dropdown_component:
    trigger: Toggle columns
