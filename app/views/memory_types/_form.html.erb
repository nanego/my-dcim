<%= render FormErrorsComponent.new(@memory_type) %>

<%= form_for(@memory_type, html: { class: "bg-body-tertiary p-4 rounded border", role: "form" }) do |f| %>
  <div class="row g-3">
    <fieldset class="col-12">
      <%= f.label :quantity, class: "form-label" %>
      <%= f.text_field :quantity, class: "form-control" %>
    </fieldset>

    <fieldset class="col-12 mt-4">
      <%= f.label :unit, class: "forml-label" %>
      <%= f.select :unit, MemoryType::UNITS, {}, class: "form-select" %>
    </fieldset>

    <div class="col-12 mt-4">
      <%= f.submit class: "btn btn-primary w-100" %>
    </div>
  </div>
<% end %>
