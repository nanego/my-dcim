<%
  breadcrumb
    .add_step(@contact, contact_path(@contact))
    .add_step(t("action.delete"))
%>

<%= render Page::HeadingComponent.new(title: t("action.delete"), breadcrumb:) %>

<div class="col-12 p-4 border-top">


  <%= render DeleteDependency::ConfirmComponent.new(@contact, contact_path(@contact, confirm: true)) %>

</div>
