<%
  title = @bay.frames.map(&:name).join(" / ")

  breadcrumb
    .add_step(title, bay_path(@bay))
    .add_step(t("action.edit"))
%>

<%= render Page::HeadingEditComponent.new(resource: @bay, title: t(".title"), breadcrumb:) %>
<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
