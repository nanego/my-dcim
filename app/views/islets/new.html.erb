<%
  provide(:title, "#{Islet.model_name.human.pluralize} | #{t("action.create")}")

  breadcrumb_steps = { Islet.model_name.human.pluralize => islets_path, t("action.create") => "" }
%>

<%= render Page::HeadingNewComponent.new(resource: @islet, title: t(".title"), breadcrumb_steps:) %>

<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
