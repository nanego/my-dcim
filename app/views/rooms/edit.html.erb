<%
  breadcrumb
    .add(Room.model_name.human.pluralize, rooms_path)
    .add(@room.name, room_path(@room))
    .add(t("action.edit"))
%>

<%= render Page::HeadingEditComponent.new(resource: @room, title: t(".title"), breadcrumb:) %>
<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
