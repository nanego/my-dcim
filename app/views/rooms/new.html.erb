<%
  breadcrumb
    .add_step(Room.model_name.human.pluralize, rooms_path)
    .add_step(t("action.create"))
%>

<%= render Page::HeadingNewComponent.new(resource: @room, title: t(".title"), breadcrumb:) %>

<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
