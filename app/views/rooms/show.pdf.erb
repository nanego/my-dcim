<div class="container-fluid">
  <% @servers_per_frames.each do |room, islets| %>
    <%= render partial: 'servers/room', locals: { room: @room, islets: islets, view_side: params[:view] } %>
  <% end %>
</div>
