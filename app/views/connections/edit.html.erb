<%= render 'layouts/breadcrumb', breadcrumb_variables: { t("visualization") => rooms_path,
                                                         @room.name => room_path(@room),
                                                         "Baie #{@frame.name}" => frame_path(@frame),
                                                         "Modification des connexions" => '' } %>

<%= render 'connections/form' %>
