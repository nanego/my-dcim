<%= form_for(@document, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @document.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="visually-hidden">Close</span></button>
      <h4><%= pluralize(@document.errors.count, "erreur", "erreurs") %> lors de la sauvegarde :</h4>

      <ul>
      <% @document.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :server_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :server_id, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
