<div class="page-header d-flex justify-content-between">
  <h1>Listing documents</h1>
  <%= link_to new_document_path, class: 'btn btn-success align-self-center' do %>
    <span class="bi bi-plus-lg"></span>
    New Document
  <% end %>
</div>

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
            <th>Matériel</th>
            <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <%= content_tag_for(:tr, @documents) do |document| %>
            <td><%= document.server_id %></td>
            <td><%= link_to 'Voir', document %></td>
        <td><%= link_to 'Edit', edit_document_path(document) %></td>
        <td><%= link_to 'Destroy', document, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %></td>
      <% end %>
    </tbody>
  </table>
</div>
