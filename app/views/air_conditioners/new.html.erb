<%
  provide(:title, "#{AirConditioner.model_name.human.pluralize} | #{t("action.create")}")

  breadcrumb_steps = { AirConditioner.model_name.human.pluralize => air_conditioners_path, t("action.create") => "" }
%>

<%= render Page::HeadingNewComponent.new(resource: @air_conditioner, title: t(".title"), breadcrumb_steps:) %>

<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
