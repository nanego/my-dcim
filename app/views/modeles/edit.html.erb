<%
  breadcrumb
    .add(Modele.model_name.human.pluralize, modeles_path)
    .add(@modele, modele_path(@modele))
    .add(t("action.edit"))
%>

<%= render Page::HeadingEditComponent.new(resource: @modele, title: t(".title"), breadcrumb:) %>
<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
