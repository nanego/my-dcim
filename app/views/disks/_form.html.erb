<%= form_for(@disk, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @disk.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@disk.errors.count, "error") %> prohibited this disk from being saved:</h4>

      <ul>
      <% @disk.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :server_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :server_id, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :disk_type_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :disk_type_id, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :quantity, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :quantity, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
