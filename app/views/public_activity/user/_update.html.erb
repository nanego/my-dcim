<% if activity.trackable == activity.owner %>
s'est connecté
<% else %>
a modifié les données d'un utilisateur
  <% if activity.trackable %>
    <%= link_to activity.trackable.to_s, activity.trackable %>
  <% else %>
    (qui a été supprimé depuis)
  <% end %>
<% end %>
