<% if activity.trackable == activity.owner %>
s'est connecté
<% else %>
  <% if activity.owner.present? %>
    a modifié les données
  <% else %>
    Modification des données
  <% end %>
  <% if activity.trackable %>
    de l'utilisateur <%= link_to activity.trackable.to_s, activity.trackable %>
  <% else %>
    d'un utilisateur (qui a été supprimé depuis)
  <% end %>
<% end %>
