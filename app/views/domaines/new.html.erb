<%
  breadcrumb
    .add(Domaine.model_name.human.pluralize, domaines_path)
    .add(t("action.create"))
%>

<%= render Page::HeadingNewComponent.new(resource: @domaine, title: t(".title"), breadcrumb:) %>

<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
