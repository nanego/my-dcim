<div class="py-3 px-4 bg-body">
  <nav aria-label="breadcrumb">
    <ol class="breadcrumb">
      <li class="breadcrumb-item">
        <%= link_to root_path, class: "link-body-emphasis" do %>
          <i class="bi bi-house-door-fill"></i>
          <span class="visually-hidden"><%= t(".home") %></span>
        <% end %>
      </li>
      <% breadcrumb.each_steps do |step| %>
        <% if step.url? %>
          <li class="breadcrumb-item">
            <%= link_to step.title, step.url, class: "link-body-emphasis fw-semibold text-decoration-none"%>
          </li>
        <% else %>
          <li class="breadcrumb-item active" aria-current="page">
            <%= step.title %>
          </li>
        <% end %>
      <% end %>
    </ol>
  </nav>
</div>
