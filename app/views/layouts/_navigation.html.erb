<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Serveurs <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'Ajouter un serveur', new_serveur_path %></li>
            <li><%= link_to 'Tous les serveurs', serveurs_grids_path %></li>
            <li class="dropdown-header">Vues spécifiques :</li>
            <li><%= link_to 'Réseau', reseau_serveurs_grids_path %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to 'Toutes les salles', baies_serveurs_path %></li>
            <% Salle.includes(:serveurs).order('title asc').each do |salle| %>
              <% if salle.title.present? && salle.serveurs.present? %>
                <li><%= link_to salle.title, salle_path(salle) %></li>
              <% end %>
            <% end %>
          </ul>
        </li>

        <%= render 'layouts/navigation_links' %>
        <%= render 'layouts/nav_links_for_auth' %>
      </ul>
    </div>
  </div>
</nav>
