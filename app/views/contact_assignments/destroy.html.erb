<%
  breadcrumb
    .add_step(@contact_assignment, contact_assignment_path(@contact_assignment))
    .add_step(t("action.delete"))
%>

<%= render Page::HeadingComponent.new(title: t("action.delete"), breadcrumb:) %>

<div class="col-12 p-4 border-top">
  <%= render DeleteDependencyComponent.new(@contact_assignment, contact_assignment_path(@contact_assignment, confirm: true)) %>
</div>
