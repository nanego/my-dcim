<%
  breadcrumb
    .add(ContactAssignment.model_name.human.pluralize, contact_assignments_path)
    .add(@contact_assignment, contact_assignment_path(@contact_assignment))
    .add(t("action.edit"))
%>

<%= render Page::HeadingEditComponent.new(resource: @contact_assignment, title: t(".title"), breadcrumb:) %>
<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
