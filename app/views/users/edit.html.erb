<%
breadcrumb
  .add_step(@user.name, user_path(@user))
  .add_step(t("action.edit"))
%>

<%= render Page::HeadingEditComponent.new(resource: @user, title: t(".title"), breadcrumb:) %>

<div class="col-12 p-4 border-top">
  <%= render partial: "form", locals: { url: user_path(@user) } %>
</div>
