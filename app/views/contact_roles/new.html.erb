<%
  provide(:title, "#{ContactRole.model_name.human.pluralize} | #{t("action.create")}")

  breadcrumb_steps = { ContactRole.model_name.human.pluralize => contact_roles_path, t("action.create") => "" }
%>

<%= render Page::HeadingNewComponent.new(resource: @contact_role, title: t(".title"), breadcrumb_steps:) %>

<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
