<%
  breadcrumb
    .add(Architecture.model_name.human.pluralize, architectures_path)
    .add(@architecture, architecture_path(@architecture))
    .add(t("action.edit"))
%>

<%= render Page::HeadingEditComponent.new(resource: @architecture, title: t(".title"), breadcrumb:) %>
<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
