<%
  breadcrumb
    .add_step(@architecture, architecture_path(@architecture))
    .add_step(t("action.delete"))
%>

<%= render Page::HeadingComponent.new(title: t("action.delete"), breadcrumb:) %>

<div class="col-12 p-4 border-top">
  <%= render DeleteDependencyComponent.new(@architecture, architecture_path(@architecture, confirm: true)) %>
</div>
