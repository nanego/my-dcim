<%
  breadcrumb
    .add_step(Architecture.model_name.human.pluralize, architectures_path)
    .add_step(t("action.create"))
%>

<%= render Page::HeadingNewComponent.new(resource: @architecture, title: t(".title"), breadcrumb:) %>

<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
