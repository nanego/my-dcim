<%
  provide(:title, "#{Frame.model_name.human.pluralize} | #{t("action.create")}")

  breadcrumb_steps = { Frame.model_name.human.pluralize => frames_path, t("action.create") => "" }
%>

<%= render Page::HeadingNewComponent.new(resource: @frame, title: t(".title"), breadcrumb_steps:) %>

<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
