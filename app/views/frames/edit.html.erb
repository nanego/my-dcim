<%
  breadcrumb
    .add(Frame.model_name.human.pluralize, frames_path)
    .add(@frame.name, frame_path(@frame))
    .add(t("action.edit"))
%>

<%= render Page::HeadingEditComponent.new(
  resource: @frame, title: t(".title"), breadcrumb:
) %>
<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
