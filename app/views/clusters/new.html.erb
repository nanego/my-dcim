<%
  breadcrumb
    .add_step(Cluster.model_name.human.pluralize, clusters_path)
    .add_step(t("action.create"))
%>

<%= render Page::HeadingNewComponent.new(resource: @cluster, title: t(".title"), breadcrumb:) %>

<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
