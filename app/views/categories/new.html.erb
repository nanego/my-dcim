<%
  provide(:title, "#{Category.model_name.human.pluralize} | #{t("action.create")}")

  breadcrumb_steps = { Category.model_name.human.pluralize => categories_path, t("action.create") => "" }
%>

<%= render Page::HeadingNewComponent.new(resource: @category, title: t(".title"), breadcrumb_steps:) %>

<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
