<div class="container">

  <div class="row">

    <div class="col-md-12 well">

      <div style="margin-bottom: 20px;">
        <h2>Liste des ports cablés</h2>
      </div>

      <% @frames.each do |frame| %>
        <h3>
          <%= link_to "#{frame.name_with_room_and_ilot}", frame_path(frame, :view => params[:view]), style: 'text-decoration: underline;' %>
        </h3>
        <% frame.servers.includes(:modele, :cards_servers => [:ports, :composant]).order('position desc').each do |s| %>
          <h4 style="display: inline;"><%= s.nom %></h4><span> (<%= s.modele.title %>)</span>
          <p>
            <% s.cards_servers.each do |c| %>
              <% c.ports.each do |port| %>
                <% if port && port.cablename && c.composant.name.present? %>
                  <%= "#{c.composant.name}#{c.composant.name.include?('SL') ? "/#{port.position}" : port.position} - #{port.network_conf(s.frame.switch_slot)}" %>
                  <br>
                <% end %>
              <% end %>
            <% end %>
          </p>
        <% end %>
      <% end %>

    </div>

  </div>

</div>
