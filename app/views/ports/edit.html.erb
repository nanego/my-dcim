<%= render 'layouts/breadcrumb', params: {"Salles" => rooms_path,
                                          @port.parent.server.frame.room.name => room_path(@port.parent.server.frame.room),
                                          "Baie #{@port.parent.server.frame.name}" => frame_path(@port.parent.server.frame),
                                          @port.parent.server.name => server_path(@port.parent.server),
                                          "Port #{@port.parent.try(:composant).try(:name)} #{@port.position}" => ''} %>

<%= render 'ports/form' %>
