<%
  breadcrumb
    .add_step(t("visualization.title"), rooms_path)
    .add_step(@port.card.server.frame.room.name, room_path(@port.card.server.frame.room))
    .add_step("Baie #{@port.card.server.frame.name}", frame_path(@port.card.server.frame))
    .add_step(@port.card.server.name, server_path(@port.card.server))
    .add_step("Port #{@port.card.try(:composant).try(:name)} #{@port.position}")
%>

<%= render partial: "layouts/breadcrumb", locals: { breadcrumb: } %>

<%= render 'ports/form' %>
