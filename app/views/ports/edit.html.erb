<%= render 'layouts/breadcrumb', breadcrumb_variables: {t("visualization") => rooms_path,
                                          @port.card.server.frame.room.name => room_path(@port.card.server.frame.room),
                                          "Baie #{@port.card.server.frame.name}" => frame_path(@port.card.server.frame),
                                          @port.card.server.name => server_path(@port.card.server),
                                          "Port #{@port.card.try(:composant).try(:name)} #{@port.position}" => ''} %>

<%= render 'ports/form' %>
