<%
  breadcrumb
    .add_step(@original_power_distribution_unit.name, power_distribution_unit_path(@original_power_distribution_unit))
    .add_step(t("action.duplicate"))
%>

<%= render Page::HeadingEditComponent.new(
      resource: @original_power_distribution_unit,
      title: t(".title", power_distribution_unit_name: @original_power_distribution_unit.name),
      breadcrumb:,
    ) %>

<div class="col-12 p-4 border-top d-flex flex-column row-gap-4">
  <div class="alert alert-warning mb-0">
    Cette section est actuellement en cours de développement. Les données peuvent être amenées à être supprimées ou altérées par des changements le temps du développement.
  </div>

  <%= render partial: "form", locals: { power_distribution_unit: @power_distribution_unit } %>
</div>
