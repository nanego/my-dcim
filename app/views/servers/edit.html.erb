<% provide(:title, "#{Server.model_name.human} | #{@server.name} | #{t(".title")}") %>
<% breadcrumb_steps = {
    Server.model_name.human => servers_path,
    @server.name => server_path(@server),
    t("action.edit") => ""
} %>

<%= render Page::HeadingEditComponent.new(resource: @server, title: t(".title"), breadcrumb_steps:) %>
<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
