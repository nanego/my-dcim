<% provide(:title, "#{t("servers.index.title")} | #{t("action.create")}") %>
<% breadcrumb_steps = {
  t("servers.index.title") => servers_path,
  t(".title") => ""
} %>

<%= render Page::HeadingComponent.new(resource: @server, title: t(".title"), breadcrumb_steps:, style: :create) %>
<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
