<%
  provide(:title, "#{Color.model_name.human.pluralize} | #{t("action.create")}")

  breadcrumb_steps = { Color.model_name.human.pluralize => colors_path, t("action.create") => "" }
%>

<%= render Page::HeadingNewComponent.new(resource: @color, title: t(".title"), breadcrumb_steps:) %>

<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
