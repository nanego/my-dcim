<%= form_for(@slot, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @slot.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@slot.errors.count, "error") %> prohibited this slot from being saved:</h4>

      <ul>
      <% @slot.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :numero, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :numero, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :serveur_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :serveur_id, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :valeur, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :valeur, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
