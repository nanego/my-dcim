<%
  breadcrumb
  .add_step(@move.decorated.display_name, moves_project_step_move_path(@moves_project_step, @move))
  .add_step(t("action.edit"))
%>

<%= render Page::HeadingNewComponent.new(
  resource: @move, title: t(".title"), breadcrumb:,
  back_button_url: moves_project_step_move_path(@moves_project_step, @move),
) %>

<div class="col-12 p-4 border-top">
  <%= render partial: "form", locals: { move: @move, moves_project_step: @moves_project_step } %>
</div>
