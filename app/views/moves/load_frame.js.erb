$("#frame").html("<%= escape_javascript(render partial: 'frames/show', locals: {frame: @frame,
                                            servers: (@frame.servers.no_pdus).sort_by { |server| server.position.present? ? server.position : 0}.reverse,
                                            settings: Frame.settings.merge({:min_height => 27 }),
                                            view_type: @view,
                                            editable: false
                                            # , highlighted_servers: [@move.moveable]
}) %>");

