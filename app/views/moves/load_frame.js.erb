$("#frame").html("<%= escape_javascript(render partial: "frames/show_frame", locals: {
  frame: @frame,
  servers: @servers,
  settings: Frame::DEFAULT_SETTINGS.merge({min_height: 27 }),
  view_type: @view,
  editable: false,
  clickable_lines: true
  # , highlighted_servers: [@move.moveable]
}) %>");

<% f = ActionView::Base.default_form_builder.new(:move, @move, self, {}) %>
