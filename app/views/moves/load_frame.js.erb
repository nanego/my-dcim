$("#frame").html("<%= escape_javascript(render partial: 'frames/show_frame', locals: {frame: @frame,
                                            servers: (@frame.servers).sort_by { |server| server.position.present? ? server.position : 0}.reverse,
                                            settings: Frame.settings.merge({:min_height => 27 }),
                                            view_type: @view,
                                            editable: false,
                                            clickable_lines: true
                                            # , highlighted_servers: [@move.moveable]
}) %>");

<% f = ActionView::Base.default_form_builder.new(:move, @move, self, {}) %>

$("#bay_size_container").html("<%=
                                  escape_javascript(
                                    render partial: 'moves/select_bay_position',
                                    locals: { f: f, move: @move, frame_size: @frame.u }
                                  )
                                %>");
