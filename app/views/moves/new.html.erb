<%
  breadcrumb
    .add_step(Move.model_name.human(count: 2), moves_path)
    .add_step(t("action.create"))
%>

<%= render Page::HeadingNewComponent.new(resource: @move, title: t(".title"), breadcrumb:) %>

<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
