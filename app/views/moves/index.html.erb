<%= render 'layouts/breadcrumb', params: {"Déplacements prévus" => ''} %>

<div class="page-header">
  <%= link_to new_move_path, class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-plus"></span>
    Prévoir un nouveau déplacement
  <% end %>
  <h1>Déplacements prévus</h1>
</div>

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
        <th></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <%= content_tag_for(:tr, @moves) do |move| %>
        <td><%= link_to "#{move.moveable.try(:modele).try(:category)} #{move.moveable} vers la baie #{move.frame}", edit_move_path(move) %></td>
        <td><%= link_to 'Supprimer', move, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-warning' %></td>
      <% end %>
    </tbody>
  </table>
</div>
