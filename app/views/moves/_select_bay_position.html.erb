<fieldset class="col-12 mt-4">
  <%= f.label :position, class: "form-label" %>
  <%= f.select :position,
               options_for_select(Array(1..frame_size).reverse, { :selected => move.position }),
               { include_blank: true },
               class: "form-select"
  %>
</fieldset>
