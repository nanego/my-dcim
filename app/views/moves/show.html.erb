<%= render 'layouts/breadcrumb', breadcrumb_steps: {"Déplacements prévus" => moves_path,
                                          "Détails" => ''} %>

<div>
  <%= link_to moves_path, class: 'btn btn-outline-secondary' do %>
    <span class="bi bi-list-ul"></span>
    Retour
  <% end %>
  <%= link_to edit_move_path(@move), class: "btn btn-info" do %>
    <span class="bi bi-pencil"></span>
    Edit
  <% end %>
  <h1>Voir move</h1>
</div>

<dl class="dl-horizontal">
</dl>
