<%
  provide(:title, "#{CardType.model_name.human(count: 2)} | #{@card_type} | #{t("action.edit")}")

  breadcrumb_steps = {
    CardType.model_name.human(count: 2) => card_types_path,
    @card_type => card_type_path(@card_type),
    t("action.edit") => ""
  }
%>

<%= render Page::HeadingEditComponent.new(resource: @card_type, title: t(".title"), breadcrumb_steps:) %>
<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
