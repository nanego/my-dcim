<%
  breadcrumb
    .add(CardType.model_name.human(count: 2), card_types_path)
    .add(t("action.create"))
%>

<%= render Page::HeadingNewComponent.new(resource: @card_type, title: t(".title"), breadcrumb:) %>

<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
