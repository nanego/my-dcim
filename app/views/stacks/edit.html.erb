<%
  breadcrumb
    .add(Stack.model_name.human.pluralize, stacks_path)
    .add(@stack, stack_path(@stack))
    .add(t("action.edit"))
%>

<%= render Page::HeadingEditComponent.new(resource: @stack, title: t(".title"), breadcrumb:) %>
<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
