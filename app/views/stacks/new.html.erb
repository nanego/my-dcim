<%
  provide(:title, "#{Stack.model_name.human.pluralize} | #{t("action.create")}")

  breadcrumb_steps = { Stack.model_name.human.pluralize => stacks_path, t("action.create") => "" }
%>

<%= render Page::HeadingNewComponent.new(resource: @stack, title: t(".title"), breadcrumb_steps:) %>

<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
