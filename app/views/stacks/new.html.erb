<%
  breadcrumb
    .add_step(Stack.model_name.human.pluralize, stacks_path)
    .add_step(t("action.create"))
%>

<%= render Page::HeadingNewComponent.new(resource: @stack, title: t(".title"), breadcrumb:) %>

<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
