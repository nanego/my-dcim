<%
  provide(:title, "#{Maintainer.model_name.human.pluralize} | #{t("action.create")}")

  breadcrumb_steps = { Maintainer.model_name.human.pluralize => maintainers_path, t("action.create") => "" }
%>

<%= render Page::HeadingNewComponent.new(resource: @maintainer, title: t(".title"), breadcrumb_steps:) %>

<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
