<%
  provide(:title, "#{Manufacturer.model_name.human.pluralize} | #{t("action.create")}")

  breadcrumb_steps = { Manufacturer.model_name.human.pluralize => manufacturers_path, t("action.create") => "" }
%>

<%= render Page::HeadingNewComponent.new(resource: @manufacturer, title: t(".title"), breadcrumb_steps:) %>

<div class="col-12 p-4 border-top">
  <%= render "form" %>
</div>
